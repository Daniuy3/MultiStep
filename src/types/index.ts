export type AddItem = {
    id: number,
    heading: string,
    price: string, 
    value:number
}

export type planItem = {
    name: string,
    price: string,
    value:number
}

export type elemento = {
    id: number;
    plan: string;
    priceM: string;
    priceY: string;
    valueM: number;
    valueY: number;
}
